import type { NextPage } from "next";

const Home: NextPage = () => (
  <main className='min-h-screen grid place-items-center p-10'>
    <h1 className='text-2xl font-semibold'>Frontend build OK </h1>
  </main>
);

export default Home;